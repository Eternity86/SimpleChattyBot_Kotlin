fun main() {
    // write your code here
    val n = readln().toInt()
    var max1 = 1
    var max2 = 1
    var curr = readln().toInt()
    if (n == 1) {
        println(curr)
        return
    }
    for (i in 2..n) {
        curr = readln().toInt()

    }
}